import { HeaderContainer, HeaderTitle, HeaderUsername } from "../styles/Header"

const Header = () => {
    return (
        <HeaderContainer>
            <HeaderTitle>
                JAB
            </HeaderTitle>
            <HeaderUsername>
                username
            </HeaderUsername>
        </HeaderContainer>
    )
}

export default Header